spring:
  application:
    name: javabank
  datasource:
    url: jdbc:postgresql://localhost:5432/javabank
    username: postgres
    password: sua_senha
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true
